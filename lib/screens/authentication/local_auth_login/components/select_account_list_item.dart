import 'package:flutter/material.dart';

class SelectAccountListItem extends StatelessWidget {
  const SelectAccountListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
